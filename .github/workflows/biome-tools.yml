name: "Biome tools"

on:
  push:
    branches: [main,master]
    paths:
      - ".github/workflows/test.yml"
      - "packages/**"
      - "package.json"
      - "pnpm-lock.yaml"
  pull_request:
    types: [opened, synchronize, reopened]
    paths:
      - ".github/workflows/test.yml"
      - "packages/**"
      - "package.json"
      - "pnpm-lock.yaml"
    branches-ignore:
      - "**/graphite-base/**"
  workflow_dispatch:

jobs:
  formatter:
    name: Formatter
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v6

      - uses: ./.github/actions/submodules
        with:
          token: ${{ secrets.LC_SUBMODULES_TOKEN }}

      - uses: biomejs/setup-biome@v2

      - name: Formatter
        run: biome format --max-diagnostics=none --colors force .

  linter:
    name: Linter
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v6

      - uses: ./.github/actions/submodules
        with:
          token: ${{ secrets.LC_SUBMODULES_TOKEN }}

      - uses: biomejs/setup-biome@v2

      - name: Linter
        run: biome check --max-diagnostics=none --colors force .
