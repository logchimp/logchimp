image:
  file: .gitpod.Dockerfile

ports:
  - port: 8080
    onOpen: open-preview
  - port: 3000
    onOpen: ignore

tasks:
  - init: bash ./scripts/gitpod-init.sh
    command: yarn run server:dev
    command: yarn run frontend:dev --host 0.0.0.0
