{
	"middleware": {
		"auth": {
			"internalServerError": "Internal server error",
			"invalidAuthHeaderFormat": "Authorization header format is \"authorization: Bearer [token]\"",
			"invalidAuthHeader": "Authorization header missing",
			"invalidToken": "Invalid JWT token"
		},
		"user": {
			"userNotFound": "User not found",
			"userBlocked": "User is blocked",
			"incorrectPassword": "Incorect password",
			"userExists": "User exists"
		}
	}
}
